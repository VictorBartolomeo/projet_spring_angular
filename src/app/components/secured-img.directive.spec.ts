import { SecuredImgDirective } from './secured-img.directive';

describe('SecuredImgDirective', () => {
  it('should create an instance', () => {
    const directive = new SecuredImgDirective();
    expect(directive).toBeTruthy();
  });
});
